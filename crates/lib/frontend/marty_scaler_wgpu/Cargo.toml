[package]
name = "marty_scaler_wgpu"
description = "A scaler and simple CRT shader for MartyPC using wgpu as a backend"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
name = "marty_scaler_wgpu"
path = "src/lib.rs"
crate-type = ["lib"]

[dependencies]
marty_frontend_common =  { path = "../marty_frontend_common", features = ["use_wgpu"] }
bytemuck.workspace = true
ultraviolet = "0.9"
wgpu.workspace = true